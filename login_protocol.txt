应用层协议：
客户端发送过来的数据格式如下
邮箱号长度 邮箱号 密码长度 密码
邮箱号长度和密码长度均为unsigned char类型，占一个字节

服务端接收到登录请求后回复给客户端一个字节的unsigned char类型数据
0 表示登录成功，1表示没有成功，密码或邮箱号错误
若用户登录成功还必须返回用户的user_id，以便后面用户传输视频到服务器时，插入视频记录到数据库中时，必须提供user_id
用户登录没成功就不需了。


传输层协议选择UDP
